name: CI/CD Service A

on:
  push:
    branches: [ main ]
    paths:
      - "service_a/**"
      - "k8s/service-a-*.yaml"
      - ".github/workflows/service-a-ci-cd.yml"
  pull_request:
    branches: [ main ]
    paths:
      - "service_a/**"
      - ".github/workflows/service-a-ci-cd.yml"

permissions:
  contents: read
  packages: write

env:
  CONTAINER_REGISTRY: ghcr.io/${{ github.repository_owner }}

jobs:
  test:
    name: Test Service A
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies and run tests
        run: |
          cd service_a
          pip install -r requirements.txt
          pytest

  build-and-push:
    name: Build and Push Docker Image (Service A)
    needs: test
    runs-on: self-hosted
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Build and push Service A image
        uses: docker/build-push-action@v4
        with:
          context: ./service_a
          push: true
          tags: |
            ${{ env.CONTAINER_REGISTRY }}/service-a:${{ steps.meta.outputs.sha_short }}
            ${{ env.CONTAINER_REGISTRY }}/service-a:latest

  deploy:
    name: Deploy Service A to Kubernetes
    needs: build-and-push
    runs-on: self-hosted
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure kubectl
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        run: |
          mkdir -p $HOME/.kube
          echo "$KUBE_CONFIG" | base64 -d > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Extract metadata
        id: meta
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Update Service A Kubernetes deployment
        run: |
          IMAGE_BASE="ghcr.io/$(echo ${{ github.repository_owner }} | tr '[:upper:]' '[:lower:]')"

          sed -i "s|\${CONTAINER_REGISTRY}|$IMAGE_BASE|g" k8s/service-a-deployment.yaml
          sed -i "s|\${IMAGE_TAG}|${{ steps.meta.outputs.sha_short }}|g" k8s/service-a-deployment.yaml

      - name: Deploy Service A
        run: |
          kubectl apply -f k8s/service-a-deployment.yaml
          kubectl apply -f k8s/service-a-service.yaml

          kubectl rollout restart deployment/service-a
          kubectl rollout status deployment/service-a